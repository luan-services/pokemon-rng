# Archived Trade Evolution probe tests

Build-number tests retained for provenance of the FR1.0 Trade Evolution investigation. Production coverage is provided by `TradeEvolutionPresetProductionTest` and the normal production runner.

These files compile in the default Java package but are not production feature entrypoints.
