# Archived Run Anywhere probes

Retained for reproducibility only. Historical CLI entrypoints remain under `Main legacy ...`.

- `RunAnywhereProbePreset` — one-map primitive from before resident reapply.
- `RunAnywhereSharedHotkeyProbe` — isolated Shared integration before catalog/planner support.
- `RunAnywhereSharedRuntime` — **REJECTED** Build 37 continuous VBlank -> relocatable RamScript design.

Current production code lives in the normal Run Anywhere / Run+Bike classes plus `PresetCatalog` and the composition planner. Do not use these probes as architecture templates.
